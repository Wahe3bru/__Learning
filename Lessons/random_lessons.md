# Random lessons I came across and should be sorted

## rearange columns in Pandas
`df`
_prints: ['F','D','E']_
`df.columns.tolist()`
_use the column names printed out to rearrange_
`df = df['D','E','F']`
